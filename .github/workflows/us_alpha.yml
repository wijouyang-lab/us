name: US-Share Alpha Radar

on:
  workflow_dispatch:
  schedule:
    # 任务1: 美股盘前选股 (北京时间 20:00，UTC 12:00)
    - cron: '0 12 * * 1-5'
    # 任务2: 美股盘后复盘 (北京时间 06:00，UTC 22:00)
    - cron: '0 22 * * 1-5'

jobs:
  run-quant:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install Libs
      # 🚀 依赖库：纯 Tushare 驱动，保留 yfinance 备用
      run: pip install pandas pandas-ta requests google-genai tushare yfinance

    - name: ⏱️ Run Script Based on Time (智能分流)
      env:
        # 👇 纯血 Tushare 环境变量，彻底抛弃受限的海外接口
        TUSHARE_TOKEN: ${{ secrets.TUSHARE_TOKEN }}
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        EMAIL_ACCOUNT: ${{ secrets.EMAIL_ACCOUNT }}
        EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
        TARGET_EMAILS: ${{ secrets.TARGET_EMAILS }}
      run: |
        CURRENT_HOUR=$(date +'%H')
        # 恢复正常：UTC 11点~15点 (北京时间 19:00~23:00) 跑盘前选股 scan.py
        if [ "$CURRENT_HOUR" -ge 11 ] && [ "$CURRENT_HOUR" -le 15 ]; then
          echo "🌙 触发【盘前选股】引擎 (北京时间 20:00)..."
          python scan.py
        # 其他时间 (比如早晨 6:00) 跑盘后复盘 review.py
        else
          echo "🌅 触发【盘后复盘】引擎 (北京时间 06:00)..."
          python review.py || true
        fi

    - name: 💾 Save Logs & Sync to GitHub
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "Alpha Bot"
        git add -A
        git diff --quiet && git diff --staged --quiet || git commit -m "Auto update US-share logs"
        git push
