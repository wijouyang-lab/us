# US Review V29 Fix

## 1. 期权正股价格不再轻易显示 N/A
- Review 的正股跟踪价格顺序调整为：当日完整日线收盘价 → 最近有效交易日收盘价 → fast_info。
- 期权 Review 额外使用 `UnderlyingPrice` 作为账本历史回填兜底。
- 9/22 FTNT 历史回填记录补入 `UnderlyingPrice=170.19`。

## 2. Scan 推荐必须按“推荐事件”独立追踪
- 旧代码按 `Ticker` 分组，会把同一股票不同日期的推荐合并。
- 典型问题：NXPI 9/21 的 Stop_Loss_Hit 与 9/22 的新 Core 推荐被合并，旧止损状态可能污染新事件。
- V29 改成 `Date + Ticker + Tag` 事件级追踪。
- Observation 同样按事件独立统计，不会再因为同一股票随后进入 Core 而消失。

## 3. Observation 当前价格与胜率
- Observation 使用统一 `price_map_today` 追踪最近有效价格。
- 没有有效样本时 KPI 显示 `N/A`，不再把“没有数据”误报成 `0.00%`。
- 一旦存在有效推荐价+当前价，Observation 当前跟踪胜率按独立推荐事件计算。

## 4. 止损触发数据质量
- 只有今天的完整 OHLC 才允许执行“今日最低价触发止损”。
- 仅有最近交易日价格时，只做当前价格/PnL 跟踪，不用旧交易日低点触发今天的止损。

## 5. 核心目的
- 历史已经止损的 NXPI 事件保持历史记录。
- 新的 NXPI 推荐是另一笔独立事件，按新推荐价重新计算跟踪，不被旧事件污染。
